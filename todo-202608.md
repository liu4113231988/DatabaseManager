# DatabaseManager Avalonia 待完善清单 · 2026-08

> 范围：依据现有 Avalonia 窗口、服务接口和迁移进度整理。已实现的连接管理、对象浏览、SQL 执行/基础提示、数据编辑、表设计、转换、对比、诊断、统计、备份、导入导出、代码和文档生成不在本清单重复列出。

## P0：投入使用前应完成
- [x] **查询执行安全基线**：已加入单次查询超时、取消按钮/取消令牌、危险 DDL/DML 二次确认，以及关闭含结果集修改标签时的保存/放弃提示。
- [x] **跨平台构建门禁**：已添加 Windows、Linux、macOS 的 Avalonia 还原、构建与测试工作流；仍需以真实图形环境和数据库完成下列发布验收。
- [x] **提供备份恢复闭环**：已提供恢复文件选择、目标覆盖确认、文件校验、取消与日志；SQL Server 使用原生 RESTORE，MySQL/PostgreSQL/Oracle 使用对应客户端工具，SQLite 在替换前自动创建安全副本。仍需在各目标数据库版本上进行实机恢复验收。
- [x] **加强 SQL 执行安全性**：已解析常见数据库驱动的错误行号并自动定位编辑器光标；危险 SQL 确认可按查询标签开关，超时、取消和影响行数反馈已具备。执行计划分析转入 P1 SQL 工作台。
- [x] **验证并稳固智能提示**：字段可在对象树未展开时按需加载；支持表别名、Schema 限定名及 `[]`、双引号、反引号标识符；对象树或连接切换时清除字段缓存，避免旧连接数据进入提示结果。

## P1：显著提升专业数据库工具体验

- [x] **差异到变更发布**：在结构/数据对比结果上生成可审阅的同步脚本，支持选择性应用、执行前预览、执行日志和可选回滚脚本。
- [x] **完善 SQL 工作台**：增加查询历史、收藏/脚本库、代码片段、格式化、执行计划/Explain、结果导出和参数化执行。
- [x] **增强导入导出**：补充 SQL、JSON、XML 等常用格式；增加列映射预览、类型/编码校验、错误行报告、可恢复的批处理进度。
- [x] **提升数据编辑可靠性**：在网格中清晰标识主键与修改状态，保存后自动刷新/定位记录，关闭时提示未保存修改，并增强复杂查询结果的可编辑性判断。

## P2：体验、扩展性与运营能力

- [x] **完善工作区布局**：已实现布局持久化（窗口大小/位置/最大化、对象树左栏宽度可拖动并记忆）与多查询页会话恢复（未保存 SQL 草稿随会话恢复）；窗口位置按多显示器屏幕钳制，保障高 DPI/多显示器稳定。Dock 拖拽/浮动按「持久化优先」决策暂缓，留待后续专项。
- [x] **主题与可访问性**：已建立颜色令牌层（亮/深/高对比三套 ThemeDictionaries，全视图硬编码颜色已令牌化），状态栏支持外观切换与 90–125% 字体缩放；结果行状态/高亮/节点图标转换器随主题适配；20 个图标工具栏按钮补齐 AutomationProperties.Name。快捷键仍为窗口级 KeyDown（未迁移 HotKey）；AtomUI 对话框深色观感需实机验收。
- [x] **对象树与大库性能**：对象浏览器顶部搜索框（深度搜索元数据并定位到树）；大目录懒分页（500/页 + 加载更多）；连接/文件夹级加载指示与双击取消；多库 Schema 枚举并发上限 4。虚拟化以懒分页方案替代（TreeView 容器依赖逻辑多，风险大）。
- [x] **任务与运行监控**：任务中心（状态栏入口 + 非模态窗口：运行中/完成/失败/取消、任务日志、跨会话历史 task-history.json）；转换/导出/导入/备份恢复/统计/脚本执行已接入（原不可取消的转换与统计现可取消）；任务完成 Toast 通知；主窗口关闭时提示运行中任务。
- [x] **扩展机制**：对象树菜单贡献者扩展点（IObjectTreeMenuContributor + 注册表）；评估文档 docs/extensibility.md（方言 UI 层近零成本的结论与各扩展场景清单、差距与后续建议）。

## 验收记录建议

每项完成后应在对应 Issue 或发布说明中记录：支持的数据库类型与版本、复现/验证步骤、截图或日志、已知限制，以及回滚方式。涉及数据库结构或数据写入的功能，还应说明权限要求和备份策略。
